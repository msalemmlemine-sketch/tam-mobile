# تقرير الإصلاحات — تدقيق سبتمبر 2026

هذا الملف يلخّص ما نُفِّذ فعليًا مقابل كل بند من بنود التدقيق الستة،
بصدق وبلا مبالغة — بما في ذلك الفجوات المتبقية المعروفة.

## 1) الحسابات المالية وتخصيص الاشتراكات — ✅ منفَّذ (بنطاق مضبوط)

- `SubscriptionCalculator.allocatePayment` جديدة: تخصيص FIFO كامل
  بأعداد صحيحة (`MoneyUtils.toMinorUnits`)، مع `DueLineItem`/
  `DueAllocation`/`PaymentAllocationResult`.
- `SubscriptionRepository._allocatePayment` (المسار الحي الفعلي عند
  تسجيل/تعديل أي دفعة) يستخدمها الآن، فالقرار الحسابي (هل اكتمل
  الشهر؟ كم تبقّى؟) بات بأعداد صحيحة حصرًا.
- **قرار نطاق واعٍ**: لم تُهاجَر أعمدة SQLite نفسها من `REAL` إلى
  `INTEGER` عبر كل الجداول (subscription_payments/dues/rates/...) —
  مهاجرة شاملة كهذه تلمس عشرات الملفات بلا مترجم Dart متاح هنا
  للتحقق، فقررنا إصلاح نقطة القرار الفعلية (حيث يحدث خطأ التقريب)
  بدل مخاطرة كسر شاشات أخرى بتغيير شامل غير مُختبَر.
- اختبارات جديدة: `test/subscription_calculator_fifo_test.dart`.

## 2) استيراد CSV والعربية — ✅ منفَّذ بالكامل

- إزالة التشكيل العربي (تشكيل U+0610-U+06ED) في `CsvNormalizer.norm`.
- `CsvNormalizer.normalizePhone`: يحوّل أي صيغة هاتف موريتاني شائعة
  إلى `+222XXXXXXXX`. مطبَّق في `member_importer.dart` وفي نموذج
  إضافة/تعديل منتسب يدويًا.
- إزالة BOM وكشف الفاصل (`,`/`;`/tab) كانا موجودين مسبقًا فعليًا.

## 3) حظر واتساب — ✅ منفَّذ على المسارين الحقيقيين

اكتُشف أن المشكلة الحقيقية أعمق مما وصفه الطلب الأصلي: لم يكن هناك
أي حلقة إرسال على الجهاز أصلًا (الكود كان معطَّلًا/غير مربوط)، بينما
كانت الحلقة الفعلية التي ترسل فورًا بلا تأخير موجودة **خادميًا** في
`` (`runالتذكيرات المحليةReminders`، عبر
`ScriptApp` trigger يومي).

- أُصلحت `runالتذكيرات المحليةReminders` في `Code.gs`: فاصل عشوائي 4–9 ثوانٍ
  بين كل رسالة + حد يومي (`WA_DAILY_LIMIT`) عبر Script Properties.
- بُني `lib/services/local reminders_dispatcher.dart` من الصفر: نفس منطق
  الفاصل العشوائي والحد اليومي على مستوى الجهاز، مع تسجيل نجاح/فشل
  كل رسالة بدون كسر الحلقة، ورُبط بـ `automated_reminder_service.dart`
  ليُرسل فعليًا (لم يكن يرسل شيئًا من قبل).

## 4) Supabase/Supabase — 🟡 منفَّذ جزئيًا (بنية سليمة، تغطية محدودة)

- `CloudService` جديد بالكامل: عميل PostgREST حقيقي (لم يكن موجودًا
  إطلاقًا — كان stub معطَّلًا رغم أن التوثيق يدّعي عكس ذلك).
- `sync_outbox` (جدول + `SyncOutbox` + `CloudSyncEngine`): كتابة
  محلية فورية دومًا، ثم رفع لاحق مع إعادة محاولة عند الفشل.
- ربط FK تلقائي (`legacy_id`) بين معرّفات SQLite المحلية وuuid
  Supabase، بدل جدول ترجمة منفصل.
- `CloudSyncEngine.pull/push` (الاستبدال الكامل للجداول) تُعطَّلان
  تلقائيًا إن كان Supabase مفعَّلًا، ويتحوَّل Drive لأرشيف
  تقارير فقط (`pushArchive`/`listArchives`/`pullArchive`، مع نقاط
  Apps Script مقابلة). **الأرشيف غير مشفَّر فعليًا بعد** — base64
  فقط، ذكرنا هذا صراحة بدل ادّعاء تشفير غير منفَّذ.
- `BackupService.restoreBackup` و`CloudSyncEngine.pull` (المسار
  القديم) يمسحان الآن `sync_outbox` ويفرضان سحبًا كاملًا قبل أي دفع
  جديد — هذا هو الإصلاح المباشر لمشكلة "الازدواجية والتضارب".

**الفجوات المعروفة المتبقية (موثَّقة، غير مخفاة):**
- outbox مربوط بـ: `districts`, `institutions`, `members`
  (إضافة/تعديل)، وإدراج `subscription_payments` فقط. تعديل/حذف
  الدفعات لا يُزامَن عمدًا (Supabase يمنعهما بالسياسات) — تحتاج ميزة
  "حركة تصحيحية" مستقلة لم تُبنَ بعد.
- الدوائر/المؤسسات التي تُنشأ ضمنيًا أثناء استيراد CSV (وليس عبر
  الشاشات العادية) لا تصل حاليًا لصف الانتظار.
- لا يوجد بعد سحب دوري تلقائي من Supabase (`fetchUpdatedSince` مبنية
  وجاهزة لكن غير مستدعاة من أي شاشة) — التركيز كان على مسار "الرفع"
  أولًا بصفته الأكثر خطورة (منع الكتابة فوق بيانات الآخرين).

## 5) RLS وسجل التدقيق — ✅ منفَّذ بالكامل

`supabase/001_tam_schema_and_rls.sql` أُعيدت كتابته بالكامل:
- دورا `admin` (وصول كامل) و`district_manager` (مقصور على دائرته
  عبر `profiles.district_id`) مضافان، مع إبقاء الأدوار القديمة
  للتوافق الخلفي.
- `subscription_payments`: لا توجد أي سياسة UPDATE أو DELETE إطلاقًا
  (الرفض الافتراضي = `USING(false)` ضمنيًا)، مع عمود
  `correction_of_id` للتصحيحات كسطور جديدة.
- `audit_logs` حقيقي عبر **Trigger** (`log_subscription_payment_change`)
  يُسجِّل من قاعدة البيانات نفسها، لا بثقة تطوّع العميل.

## 6) التخزين الآمن والمفاتيح — ✅ كان سليمًا إلى حدٍّ كبير، أُكمِلت الفجوة

- `secure_kv_store.dart` و`drive_sync_config.dart` و`local reminders_service.dart`
  كانت تستخدم `flutter_secure_storage`/`--dart-define` بشكل صحيح
  مسبقًا.
- الفجوة الوحيدة: `cloud_config.dart` كان ملفًا تعويضيًا معطَّلًا رغم
  أن التوثيق يذكر متغيرات `SUPABASE_URL`/`SUPABASE_PUBLISHABLE_KEY` —
  أُصلح ليقرأهما فعليًا عبر `--dart-define`.
