# TAM Mobile — Google Drive (أرشيف التقارير فقط الآن)

⚠️ **تحديث معماري (2026-09):** كان هذا الملف يذكر سابقًا "لا يوجد
Supabase"، بينما ذكر `CLOUD_SETUP.md` في نفس الوقت أن طبقة Supabase
"جاهزة" — وهذا التناقض بين الوثيقتين كان انعكاسًا لمشكلة حقيقية في
الشيفرة: `cloud_service.dart` كان مجرد "stub" معطَّل (`enabled =>
false`) رغم أن جدول Supabase/RLS كان موجودًا فعليًا في `supabase/`. تم
حل هذا التناقض الآن: **Supabase مصدر الحقيقة الوحيد** للبيانات
المشتركة الحيّة (المنتسبون، الدفعات، ...)، وهذا الملف يوثِّق الدور
**الجديد والوحيد المتبقي** لـ Google Drive.

## الدور الحالي لـ Google Drive

- تخزين نُسخ أرشيفية من التقارير المُصدَّرة (PDF/Excel) فقط — عبر
  `DriveSyncService.pushArchive/listArchives/pullArchive`.
- **لم يعد** يُستخدم لاستعادة كاملة لجداول العمل الحية: إن كان
  Supabase مفعَّلًا في هذا البناء (`SUPABASE_URL`/`SUPABASE_PUBLISHABLE_KEY`
  ممرَّرتين عبر `--dart-define`)، فإن `DriveSyncService.pull()/push()`
  القديمتين (استبدال كامل للجداول) تُعيدان رفضًا صريحًا بدل التنفيذ،
  لمنع بالضبط مشكلة Split-Brain التي كانت موجودة: استعادة لقطة Drive
  كاملة كانت قد تمحو الجداول المحلية وتُنشئ مفاتيح جديدة تصطدم لاحقًا
  بما رُفع فعلًا لـ Supabase.
- **بدون** Supabase مفعَّل (منظمات لم تُهاجر بعد): تستمر `pull/push`
  بالعمل تمامًا كالسابق (توافق خلفي كامل) كمزامنة حية عبر JSON واحد
  + `revision` + `LockService` كما كانت مصممة أصلًا.

ابدأ من `google_apps_script/README_AR.md` لإعداد الـ Web App (أضيفت
له الآن أيضًا نقاط `pushArchive`/`listArchives`/`pullArchive`).

## التذكيرات الآلية

التذكيرات المحلية (إشعار الجهاز) لا تعتمد على Google Drive، كما كانت.
أما إرسال رسائل واتساب التذكيرية للمنتسبين المتأخرين أنفسهم فتلقائي
الآن عبر مسارين متكاملين (انظر `WHATSAPP_AUTOMATION_SETUP.md`):
جدولة داخل التطبيق (`WhatsAppDispatcher`) وجدولة خادمية عبر Apps
Script (`runWhatsAppReminders`) — كلاهما يستخدم الآن فاصلًا عشوائيًا
4–9 ثوانٍ بين كل رسالة لتفادي حظر الرقم من طرف Meta، بعد أن كانت
حلقة الخادم القديمة ترسل بلا أي تأخير.
