# تشغيل TAM Mobile على ثلاثة هواتف

التطبيق الآن يحتوي على طبقة صلاحيات محلية، وتم تجهيز طبقة الربط السحابي عبر Supabase.
حتى تصبح الهواتف الثلاثة قاعدة بيانات واحدة فعلية، يجب إنشاء مشروع Supabase واحد للنقابة ثم تشغيل الملف:

`supabase/001_tam_schema_and_rls.sql`

## الحسابات

- `organization` — أمين التنظيم — إدارة المنتسبين، التجميد/الحذف، التقارير والتحليلات، وإضافة/تعديل/تصحيح الدفعات.
- `finance` — أمين المالية — إضافة الدفعات فقط.
- `captain` — النقيب الجهوي — سحب/تصدير اللوائح فقط.

الحسابات المحلية الأولية في النسخة الحالية:
- organization / `TAM@2026Org`
- finance / `TAM@2026Fin`
- captain / `TAM@2026Cap`

يجب تغيير كلمات المرور عند أول دخول.

## بناء النسخة المتصلة

مرر قيم Supabase أثناء البناء:

`--dart-define=SUPABASE_URL=...`

`--dart-define=SUPABASE_PUBLISHABLE_KEY=...`

بدون هذه القيم يعمل التطبيق في وضع SQLite المحلي فقط.

## ملاحظة مهمة

صلاحيات الواجهة مطبقة داخل التطبيق، وصلاحيات قاعدة البيانات السحابية محددة أيضًا بواسطة RLS في ملف SQL. لا تعتمد الحماية على إخفاء الأزرار وحده.
