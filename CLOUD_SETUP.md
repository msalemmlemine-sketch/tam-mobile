# تشغيل TAM Mobile على ثلاثة هواتف عبر Supabase

⚠️ **تصحيح مهم (2026-09):** كان هذا الملف يذكر أن "طبقة الربط
السحابي عبر Supabase" جاهزة، بينما كانت `lib/services/cloud_service.dart`
فعليًا مجرد ملف تعويضي (`enabled => false`) بلا أي اتصال حقيقي —
والمزامنة الحية الفعلية كانت تمر بالكامل عبر Google Drive/Apps
Script (`DriveSyncService`). هذا التناقض بين التوثيق والشيفرة هو ما
سبَّب لبس "أيّهما مصدر الحقيقة؟". تم الآن بناء تكامل Supabase حقيقي؛
هذا الملف محدَّث ليطابق الشيفرة الفعلية.

## المعمارية الحالية

1. **SQLite محليًا**: كل كتابة تنجح فورًا محليًا بلا انتظار إنترنت
   (Local-First حقيقي).
2. **صف انتظار (Outbox)**: كل كتابة على جدول مشترك (حاليًا:
   `districts`, `institutions`, `members` (إضافة/تعديل)، وإدراج
   `subscription_payments` فقط — انظر أدناه) تُضاف لجدول
   `sync_outbox` المحلي. المفاتيح الأجنبية (district_id على
   institutions/members، وmember_id على subscription_payments) تُترجم
   تلقائيًا من رقم SQLite المحلي إلى uuid السحابي المقابل عبر
   `legacy_id` وقت الرفع (`CloudService._resolveLegacyId`)؛ إن لم يكن
   الجدول الأب قد وصل بعد، يُعاد المحاولة تلقائيًا في التشغيلة
   التالية دون فقدان العملية.
   ⚠️ **معروف وغير مكتمل بعد**: الدوائر/المؤسسات التي تُنشأ تلقائيًا
   أثناء استيراد CSV (member_importer.dart، أسماء غير موجودة مسبقًا)
   تُكتب حاليًا مباشرة بدون المرور عبر DistrictRepository/
   InstitutionRepository، فلا تُضاف لصف الانتظار — تبقى محلية فقط
   حتى تُعاد كتابتها لاحقًا عبر شاشة إدارة المؤسسات العادية، أو تُبنى
   لهذا المسار مزامنة صريحة.
3. **Supabase = مصدر الحقيقة الوحيد للبيانات الحيّة المشتركة**:
   `CloudSyncEngine` يُفرِّغ `sync_outbox` عبر `CloudService` (عميل
   PostgREST مباشر، بدون حزمة supabase_flutter) عند كل فتح للتطبيق
   (`RootShell.initState`). الربط بين المعرّف المحلي والسحابي عبر
   عمود `legacy_id` (UUID للسحابة، INTEGER محليًا)، فالرفع
   idempotent دومًا (`on_conflict=legacy_id`).
4. **Google Drive**: أرشيف تقارير فقط الآن (PDF/Excel) — لم يعد
   قناة مزامنة حيّة إن كان Supabase مفعَّلًا. انظر `DRIVE_SYNC_SETUP.md`.
5. **نسخة احتياطية محلية (BackupService)**: تبقى كما كانت (ملف .db
   + مشاركة يدوية)، لكن أي استعادة منها الآن تمسح `sync_outbox`
   المعلَّق تلقائيًا وتفرض سحبًا كاملًا من Supabase قبل أي دفع جديد،
   لمنع كتابة بيانات محلية قديمة فوق حالة سحابية أحدث.

## قيد مهم: الدفعات المالية غير قابلة للتعديل في Supabase

سياسات RLS الجديدة (`supabase/001_tam_schema_and_rls.sql`) **تمنع
UPDATE وDELETE على `subscription_payments` نهائيًا** — حتى لحساب
admin. لذلك:
- `SubscriptionRepository.recordPayment` (إدراج) هو الوحيد الذي
  يُزامَن تلقائيًا لـ Supabase.
- `updatePayment`/`deletePayment` يبقيان يعملان محليًا فقط حاليًا
  (كما كانا قبل هذا التحديث) — تصحيح دفعة على مستوى Supabase يتطلب
  إدراج سطر تصحيحي جديد (`correction_of_id`) بدل تعديل السطر
  الأصلي؛ ميزة إنشاء هذا السطر التصحيحي تلقائيًا من واجهة "تعديل
  دفعة" **لم تُبنَ بعد** — هذا معروف ومُوثَّق هنا عمدًا بدل ادّعاء
  اكتماله.

## إنشاء مشروع Supabase

شغّل الملف كاملًا في SQL Editor:

`supabase/001_tam_schema_and_rls.sql`

## الحسابات (الأدوار)

- `admin` / `administrator` — وصول كامل لكل الدوائر والفروع (الاسمان
  متكافئان تمامًا في كل سياسات RLS).
- `district_manager` — **جديد**: مقصور على دائرته فقط
  (`profiles.district_id`)، لكل من المنتسبين والدفعات المرتبطة بهم.
- `organization_secretary` — إدارة المنتسبين، التجميد، التقارير،
  وإضافة/تصحيح الدفعات (عبر إدراج سطر جديد — انظر أعلاه)، لكل
  الدوائر.
- `finance` — إضافة الدفعات فقط.
- `captain` — سحب/تصدير اللوائح فقط.

الحسابات المحلية الأولية في النسخة الحالية:
- organization / `TAM@2026Org`
- finance / `TAM@2026Fin`
- captain / `TAM@2026Cap`

يجب تغيير كلمات المرور عند أول دخول.

## بناء النسخة المتصلة

مرر قيم Supabase أثناء البناء:

`--dart-define=SUPABASE_URL=...`

`--dart-define=SUPABASE_PUBLISHABLE_KEY=...`

بدون هذه القيم يعمل التطبيق في وضع SQLite المحلي + Google Drive
(المزامنة الحيّة القديمة تبقى تعمل — انظر `DRIVE_SYNC_SETUP.md`)،
فلا ينكسر أي بناء أو منظمة لم تُهاجر بعد.

## ملاحظة مهمة

صلاحيات الواجهة مطبقة داخل التطبيق، وصلاحيات قاعدة البيانات السحابية
محددة أيضًا بواسطة RLS في ملف SQL — بما في ذلك سجل تدقيق (`audit_logs`)
يُكتب تلقائيًا من قاعدة البيانات نفسها (Trigger) لكل عملية على
الدفعات، وليس فقط بثقة أن العميل سيُسجِّلها بنفسه. لا تعتمد الحماية
على إخفاء الأزرار وحده.
