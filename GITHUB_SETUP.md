# رفع المشروع على GitHub وبناء APK موقَّع — من الهاتف بالكامل

هذا الدليل لا يفترض وجود حاسوب. كل خطوة تُنفَّذ من هاتفك (متصفح أو
تطبيق GitHub أو Termux).

## الفكرة العامة

المستودع لا يحتوي على مجلد `android/` (أزلناه عمدًا — انظر السبب في
`.gitignore`). عند كل رفع (push) إلى الفرع `main`، يقوم **GitHub
Actions** (خادم بناء مجاني تابع لـ GitHub، يعمل في السحابة) تلقائيًا
بـ:
1. تثبيت Flutter وJava.
2. توليد مجلد `android/` طازجًا ومطابقًا لأحدث إصدار Flutter.
3. توقيع التطبيق بمفتاح حقيقي (تم توليده لك مسبقًا — انظر أدناه).
4. بناء ملف `app-release.apk` موقَّع.
5. نشره في قسم **Releases** بالمستودع — رابط تنزيل مباشر يعمل من
   متصفح الهاتف بلا تسجيل دخول.

**لست بحاجة لتثبيت Flutter أو Android Studio على هاتفك إطلاقًا.**

---

## الخطوة 1 — إنشاء حساب ومستودع GitHub

1. من متصفح الهاتف: `github.com` → أنشئ حسابًا إن لم يكن لديك.
2. اضغط **+** أعلى الصفحة → **New repository**.
3. اسم المستودع مثلًا: `tam-mobile`.
4. اجعله **Private** إن أردت سريته (يعمل GitHub Actions مجانًا في
   المستودعات الخاصة أيضًا ضمن حد شهري سخي).
5. **لا** تُفعِّل "Add a README file" (سنرفع مشروعًا جاهزًا).
6. اضغط **Create repository**.

## الخطوة 2 — رفع ملفات المشروع

اختر إحدى الطريقتين حسب هاتفك:

### أ) أندرويد — عبر تطبيق Termux (الأنسب لعدد الملفات الكبير هنا)

1. ثبّت **Termux** من F-Droid (أفضل من نسخة Play Store القديمة).
2. داخل Termux:
   ```bash
   pkg update && pkg install git -y
   termux-setup-storage   # للسماح بالوصول لملفات التنزيلات
   ```
3. أنشئ رمز دخول شخصي (Personal Access Token) من GitHub بدل كلمة
   المرور (GitHub لا يقبل كلمة المرور العادية لعمليات git):
   - من المتصفح: الصورة الشخصية → **Settings** → **Developer settings**
     → **Personal access tokens** → **Tokens (classic)** → **Generate
     new token** → صلاحية `repo` فقط → انسخ الرمز الناتج (يظهر مرة
     واحدة فقط، احفظه مؤقتًا).
4. استنسخ المستودع الفارغ وأضف ملفات المشروع (بعد فك ضغط الملف
   المُرفَق `tam_mobile.zip` داخل مجلد التنزيلات):
   ```bash
   cd storage/downloads
   unzip tam_mobile.zip
   git clone https://github.com/USERNAME/tam-mobile.git
   cp -r tam_mobile/. tam-mobile/
   cd tam-mobile
   git add -A
   git commit -m "رفع مشروع تام موبايل"
   git branch -M main
   git push -u origin main
   ```
   عند طلب اسم المستخدم وكلمة المرور: استخدم اسم مستخدم GitHub،
   وألصق **رمز الدخول الشخصي** (وليس كلمة مرور حسابك) في خانة كلمة
   المرور.

### ب) آيفون — عبر تطبيق Working Copy

1. ثبّت تطبيق **Working Copy** (نسخته المجانية كافية لهذا).
2. من داخل التطبيق: أنشئ مستودعًا جديدًا واربطه بحسابك على GitHub
   (يدعم تسجيل الدخول المباشر، لا حاجة لرمز يدوي).
3. استورد ملفات `tam_mobile.zip` (فك ضغطه أولًا عبر تطبيق الملفات)
   داخل مجلد المستودع من داخل Working Copy.
4. اعمل Commit ثم Push للفرع `main`.

### ج) بديل عام (أي هاتف) — رفع عبر متصفح GitHub مباشرة

من صفحة المستودع: **Add file** → **Upload files** → اسحب مجلد
المشروع بعد فك ضغطه (المتصفحات الحديثة تدعم سحب مجلد كامل مع بنيته)
→ **Commit changes**. أبطأ وأقل موثوقية للمشاريع الكبيرة، لكنه يعمل
بلا أي تطبيق إضافي.

---

## الخطوة 3 — إضافة أسرار التوقيع (Secrets)

من صفحة المستودع: **Settings** → **Secrets and variables** →
**Actions** → **New repository secret**. أضف الأربعة التالية
(القيم الحقيقية في الملفات المرفقة معك في هذه المحادثة):

| اسم الـ Secret | القيمة |
|---|---|
| `KEYSTORE_BASE64` | محتوى ملف `tam-release-key.jks.base64.txt` كاملًا (انسخه والصقه) |
| `KEYSTORE_PASSWORD` | من ملف `signing-credentials.txt` |
| `KEY_PASSWORD` | نفس قيمة `KEYSTORE_PASSWORD` أعلاه (انظر الشرح في الملف) |
| `KEY_ALIAS` | `tam_upload` |

⚠️ **مهم جدًا**: احتفظ بملف `tam-release-key.jks` وملف
`signing-credentials.txt` في مكان آمن دائم (تخزين سحابي خاص، أو
مدير كلمات مرور) — **هذا هو مفتاح هوية توقيع تطبيقك للأبد**. لو
فقدته، لن تستطيع لاحقًا نشر تحديث للتطبيق يُثبَّت فوق النسخة الحالية
على هواتف المستخدمين (سيضطرون لحذف التطبيق القديم وتثبيت الجديد من
الصفر). لا ترفعه أبدًا كملف داخل المستودع نفسه — `.gitignore` يمنع
ذلك تلقائيًا، لكن انتبه ألا تُلغي هذا المنع لاحقًا.

## الخطوة 4 — تشغيل البناء

بعد رفع الملفات (Push)، سيبدأ البناء تلقائيًا. للتأكد أو لتشغيله
يدويًا:

1. من صفحة المستودع → تبويب **Actions**.
2. اختر workflow باسم **"بناء APK موقَّع"**.
3. إن لم يبدأ تلقائيًا: **Run workflow** → **Run workflow** (زر
   أخضر).
4. انتظر 5–10 دقائق (أيقونة صفراء دوّارة ← تتحول لعلامة ✔ خضراء).

## الخطوة 5 — تنزيل الـ APK وتثبيته

1. من صفحة المستودع الرئيسية → **Releases** (في العمود الجانبي، أو
   `github.com/USERNAME/tam-mobile/releases`).
2. افتح آخر إصدار → نزّل `app-release.apk`.
3. عند فتح الملف: قد يطلب منك أندرويد تفعيل "تثبيت من مصادر غير
   معروفة" لتطبيق المتصفح/إدارة الملفات — فعّله مرة واحدة فقط، ثم
   ثبّت التطبيق عاديًا.

---

## عند تعديل الكود لاحقًا

كرّر: عدِّل الملفات (من Termux/Working Copy مباشرة أو بمساعدتي)،
ثم `git add -A && git commit -m "..." && git push` — سيُعاد البناء
والتوقيع والنشر تلقائيًا في كل مرة، بنفس مفتاح التوقيع، فتُثبَّت
التحديثات فوق النسخة القديمة مباشرة بلا مشاكل.

---

## بناء نسخة iPhone (iOS) — بنفس أوامر Termux أعلاه بالضبط

لا حاجة لأي خطوة إضافية على الهاتف: نفس `git push` أعلاه يشغّل **كلا**
الـ Workflow، بناء أندرويد (`build-signed-apk.yml`) وبناء iOS
(`build-ios.yml`)، لأن كليهما مضبوط على نفس الفرع `main`. الفرق الوحيد
لِـ iOS هو Secrets توقيع Apple المنفصلة (اسمها يبدأ بـ `IOS_`)، وهي
موثَّقة بالتفصيل الكامل — بما تحتاجه من Apple بالضبط، وكيف تحصل عليه،
وطريقة تثبيت الملف الناتج على آيفون أمين المالية دون Mac — في ملف
[`IOS_SIGNING.md`](./IOS_SIGNING.md) في جذر هذا المستودع.

باختصار:
1. من تبويب **Actions** بعد أي push: workflow باسم **"بناء iOS"** يعمل
   تلقائيًا. وظيفته الأولى (تحقّق فقط، بلا توقيع) تعمل حتى بدون أي
   Secret من Apple.
2. وظيفته الثانية (IPA موقَّع فعليًا وقابل للتثبيت) تُتخطى تلقائيًا
   إلى أن تضيف Secrets Apple المذكورة في `IOS_SIGNING.md` — عندها
   فقط ينتج IPA حقيقي في تبويب **Releases**، بنفس الطريقة التي يظهر
   بها APK أندرويد.
